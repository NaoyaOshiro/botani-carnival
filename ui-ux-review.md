# UI/UX レビュー — BOTANI CARNIVAL 特設サイト

レビュー日: 2026-07-20
対象コミット: `46e9184`（ヒーロー上部のバンティングフラッグを削除）
評価基準: 「動く / 動かない」ではなく **「来場者が増えるか」**

---

## 総評

デザインの完成度は高い。特に直近のエディトリアル刷新（イベント情報・協賛）は品があり、
このサイトの正解の方向。

ただし、致命的な問題が3つある。

1. **サイトの目的（＝来場）に対する導線が1本もない**
2. **本番にダミーデータが出ている**
3. **初期ロード約20MB。スマホで見られない可能性がある**

---

## 🔴 Critical — 公開前に直すべき

### 1. ダミーの協賛企業が本番公開されている

`client/src/components/sections/SponsorsSection.tsx:13`
「スポンサー企業 A / B / C」が `url: "#"` のまま公開中。

架空の協賛社名を掲示している状態で、イベントの信頼性に直結する。
**実データが揃うまではセクションごと非表示**（ナビ項目も含めて）にするのが安全。

### 2. 画像が重すぎる — 初期ロード約20MB

| ファイル | 実サイズ | 表示サイズ | 問題 |
|---|---|---|---|
| `logo-mark.webp` | **892KB** | 40×40px | ナビとフッターで毎回。500倍のオーバースペック |
| `flyer-pc.png` | **3.1MB** | 全幅 | PNG。LCP要素がこれ |
| `booth-map.webp` | 1.0MB | ×2枚 | しかも中身は「準備中」 |
| 出店業者画像 | **14MB / 84枚** | 2列サムネ | `loading="lazy"` なし |

出店業者カルーセルは全 `CarouselItem` をDOMに置くので、
**43枚のカードの画像84枚がすべて即時ダウンロード**される
（`client/src/components/sections/ExhibitorsSection.tsx:108`）。
`loading="lazy"` は `kitchen-car.webp` の1枚にしか付いていない。

沖縄のイベント、当日会場でスマホから見る人が多いはず。
**モバイル回線で20MBは事実上開けない。**

対策（効果順）:

- 全 `<img>` に `loading="lazy"` / ヒーローだけ `fetchpriority="high"`
- `logo-mark` を 96px の webp に再書き出し（→ 約5KB、**99%削減**）
- flyer を PNG→WebP、幅1920pxに（→ 3.1MB が 300KB程度に）
- PC/SP フライヤーは `<picture><source media>` に。
  今は `hidden md:block` なので **両方ダウンロードされている**

### 3. OGP画像がない

`client/index.html` に `og:` タグが1つもない。
主要な集客経路が Instagram DM / ストーリーズなのに、
**シェアしてもリンクカードが真っ白**になる。
フライヤーが最強のクリエイティブなのに使えていない。ここは費用対効果が一番高い。

あわせて `index.html` に構造の壊れがある:

```html
<html lang="en">        <!-- 実際に効いているのはこちら（英語扱い） -->
  <head>
    <html lang="ja">    <!-- head内にネストされた不正なタグ -->
```

さらに `maximum-scale=1` はピンチズームを禁止する指定で、**WCAG違反**
（弱視の来場者が拡大できない）。削除する。

---

## 🟠 High — UXの根幹

### 4. 「行く」ための導線が存在しない

このサイトを見た人が取るべき行動は
「日程を空ける」「場所を知る」「主催をフォローする」の3つだが、
**そのどれにも入口がない。**

- ファーストビューはフライヤー画像のみ。
  **日時も場所もテキストとして存在しない**（画像の中だけ）
- 追従CTAなし
- カレンダー追加なし
- 地図リンクは、スクロールしないと出会えない場所に1つだけ

最低限、次のどちらかを入れたい:

- **ヒーロー直下に固定の情報バー**:
  `8.29 SAT – 8.30 SUN｜西原さわふじ広場｜入場無料` ＋ `[地図] [カレンダーに追加] [Instagram]`
- **モバイル追従CTA**: 画面下に「マップで開く」

`.ics` の生成は静的サイトでも data URI で完結するので、実装コストは低い。

### 5. ページに `<h1>` がない

トップページの見出しは全部 `<h2>`。
イベント名がドキュメント構造上どこにも存在しない。
「珍奇植物市場 vol9」で検索した人に届かない。

ヒーローに視覚的に隠した `<h1>` を置くだけで解決する（`sr-only`）。
ついでにフライヤーの `alt` を「フライヤー」ではなく
**日時・会場を含む説明文**にすれば、画像が読めない環境でも情報が伝わる。

### 6. アンカーリンクが固定ヘッダーの下に潜る

ナビの6項目すべてが該当。`scroll-margin-top` の指定がないため、
**「イベント情報」を押すと見出しがヘッダーに隠れた位置で止まる。**

```css
section[id] { scroll-margin-top: 5rem; }
html { scroll-behavior: smooth; }
```

これだけ。ナビが6項目もある以上、優先度は高い。

### 7. 出店業者カードがキーボードで開けない

`client/src/components/sections/ExhibitorsSection.tsx:155`
`<Card onClick>` なので `tabIndex` も `role` もない。
**43件の詳細すべてがキーボード/スクリーンリーダーから到達不能。**

ただし内側に Instagram リンクとカルーセル矢印があるので、
`<button>` で全体を包むとネストが不正になる。
正しい形は「カード内の屋号をボタンにして、カード全体は `::after` で当たり判定を広げる」パターン。

### 8. ブース配置図セクションが実質空

同じ「準備中」画像を2回、1MBずつ。ナビにも項目がある。
**期待して押した人が2回がっかりする構造。**

配置図が出るまでは、セクションを1ブロックに畳んで
「配置図は公開まで今しばらくお待ちください（8月上旬公開予定）」と
テキストのみにするほうが誠実で、かつ軽い。

---

## 🟡 Medium — 完成度

### 9. デザイン言語が2つ混在している

刷新が途中で止まっている。

| セクション | 様式 |
|---|---|
| イベント情報・協賛 | ✅ **エディトリアル**（Overline / 明朝見出し / 罫線） |
| ブース配置図・出店業者・FAQ・お問い合わせ | ⚠️ **旧**（🗺️絵文字 + 中央寄せ + 下線バー） |

7セクション中4つが旧様式。上から読むと、
**イベント情報で「洗練された」と感じた直後に絵文字装飾に戻る**ので、
刷新分がむしろ浮いて見える。残り4つも揃えるのを強く推奨。

### 10. 日程の色が場所によって食い違う

日付の色分けは、このサイトで最も重要な情報設計。それが壊れている。

| | 8/29（土） | 8/30（日） |
|---|---|---|
| 出店業者 / イベント情報 | 🟢 緑 `0.42 0.16 145` | 🔴 赤 `0.55 0.22 0` |
| **ブース配置図** | 🟠 **オレンジ** `0.72 0.18 55` | 🔴 赤 `0.60 0.22 0`（**明度も別値**） |

同じ「8月29日（土）」が、セクションによって緑だったりオレンジだったりする。
ユーザーは色を手がかりに日程を見分けるので、これは実害のある不整合。

根本原因は、`index.css` に `--carnival-green` などのトークンが定義済みなのに、
**全コンポーネントが oklch のリテラルをベタ書き**していること。
`--day-1` / `--day-2` をトークン化して全箇所から参照する形に寄せる。

### 11. `.reveal` の初期値が `opacity: 0`

`client/src/index.css:170`
JSが落ちた場合や、`#exhibitors` などURLハッシュ付きで直接開いて
IntersectionObserverの登録が間に合わなかった場合、
**コンテンツが永久に見えない。**

また `prefers-reduced-motion` に非対応。
前庭障害のあるユーザー向けに、モーションを止める指定で
`.reveal` を即 `opacity:1` にするガードを入れておくと安全
（副次的に、上記の「消える」リスクの保険にもなる）。

### 12. スケルトンの400msは、いま逆効果かもしれない

`client/src/components/sections/ExhibitorsSection.tsx:352`
データはローカルなので純粋な演出遅延だが、`count={list.length}` なので
**43枚ぶんのスケルトンをマウント→400ms後に破棄→43枚のカードを再マウント**している。
ローエンド端末ではこの往復自体がカクつきの原因になり得る。

「切り替わったと分からせる」目的なら、
**上位12枚だけスケルトンにする**（下は見えていない）だけで体感は同じで、コストは1/4。

### 13. 現在地が分からない / タブに戻れない

- ナビ6項目にアクティブ表示がなく、長いページのどこにいるか分からない
- 出店業者43枚のグリッドは非常に長く、
  **下までスクロールすると日程タブに戻る手段がない** → `sticky top-16` を推奨

### 14. モバイルの出店業者カードの情報量

- 屋号 **11px**（iOSの実用下限は12px程度）＋ `truncate` で1行 → 長い屋号がほぼ読めない
- 説明文がモバイルで `line-clamp-1`

**屋号は説明文より重要。**
もし削るなら、説明文をモバイルで完全に隠して、
屋号を12pxの2行 clamp にするほうが情報の優先順位として正しい。

### 15. カードの中にカルーセルは要るか

2列表示のサムネイルで左右矢印を押す人はほとんどいない。
にもかかわらず、これが **画像を全枚数プリロードさせている元凶**（項目2）であり、
カード全体のタップ領域も削っている。

**カードは1枚目固定、カルーセルはモーダルの中だけ**にすれば、
UXはほぼ変わらず転送量が1/2〜1/3になる。

---

## 🔵 Low

- `client/src/index.css:137` の `.flex { min-height:0; min-width:0 }` —
  **全 flex 要素へのグローバル介入**で、
  将来 sticky やスクロールコンテナを入れたときに原因不明の不具合になる
- 波線の `mt-16 -mb-24` がセクションごとに手打ち（`-mb-20` / `-mb-24` が混在）
- 波線SVGに `aria-hidden="true"` を付ける
- フッターに Instagram リンクがない（最終スクロール地点なのに導線が切れる）
- Noto Serif JP の 700/900 は日本語フル。
  `&text=` サブセットか、見出し専用に絞ると数百KB削れる

---

## 推奨する着手順

| # | 対応 | 効果 | 工数 |
|---|---|---|---|
| 1 | ダミー協賛を非表示 | 信頼性 | 5分 |
| 2 | OGP + `html lang` + `maximum-scale` 削除 | 集客に直結 | 15分 |
| 3 | logo/flyer リサイズ + 全画像 lazy | **20MB → 1MB台** | 1時間 |
| 4 | `scroll-margin-top` | ナビ全項目が正常化 | 5分 |
| 5 | ヒーロー下の情報バー（日時・場所・地図） | CV | 1時間 |
| 6 | 日程カラーのトークン化 | 情報設計 | 30分 |
| 7 | 残り4セクションをエディトリアル調に統一 | 完成度 | 数時間 |

**1〜4は合計2時間弱で、体験が別物になる。** ここから着手することを推奨。
